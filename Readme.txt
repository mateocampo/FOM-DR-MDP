Para correrlo primero hay que guardar en la carpeta memoria/nominal las matrices y_{i,s}. El script lab hace esto

La función del algoritmo está en fom.py. Se llama FOM. Se corre después de tener las nominales guardadas. Esta función 
retorna x_final, la cual es la política. En este momento no estoy seguro de si funciona correctamente el algoritmo.